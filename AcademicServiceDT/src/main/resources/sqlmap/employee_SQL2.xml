<?xml version="1.0" encoding="UTF-8"?> 
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd"> 
<mapper namespace="com.oopsw.dt.MemberDAO2"> 

	<select id="login" resultType="java.lang.String"> 
		select student_name from students where student_id=#{studentId} and password=#{password}
	</select>	
	<select id="userInfo" resultType="java.lang.String">
		SELECT s.student_name, s.email, m.college, m.major_name, s.entrance_date, s.birthday, address, s.statement FROM students s, majors m WHERE s.student_id = #{studentName} AND s.major_code = m.major_code
	</select>
</mapper>